const page = () => {
  return (
    <div>
      <ul>
        <li>메타 데이터는 서버 컴포넌트에서만 존재 가능!</li>
        <li>메타 데이터는 서버 컴포넌트에서만 존재 가능!</li>
        <li>메타 데이터는 서버 컴포넌트에서만 존재 가능!</li>
        <li>메타 데이터는 서버 컴포넌트에서만 존재 가능!</li>
      </ul>
    </div>
  );
};

export default page;
